#include <iostream>
using namespace std;

size_t getSize(double *);
int main(){
	double array[20];
	cout<<"\nNumber of bytes in array: "<< sizeof(array);
	cout<<"\nNumber of bytes returned by getSize: "
	 <<getSize(array)<<endl;
}
size_t getSize(double *ptr) {
	return sizeof(ptr);
}
