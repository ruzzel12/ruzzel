#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

int main(){
	int number,timeValue;
	timeValue =time(NULL);
	srand(timeValue);
	for(int x = 0; x < 20; x++){
		cout<<"Number"<<endl<<x<<"Random"<<endl
		    <<number<<"\n"<<endl;
	}
}
